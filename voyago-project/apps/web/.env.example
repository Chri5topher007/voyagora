# Copy to .env for local development (Vite reads .env automatically).
# For production builds, VITE_API_URL is passed as a Docker build arg instead
# (see ../../docker-compose.yml and Dockerfile).
VITE_API_URL=http://localhost:3000
